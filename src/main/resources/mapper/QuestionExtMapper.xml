<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Jun 21 22:06:13 CST 2020.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    <result column="creator" jdbcType="BIGINT" property="creator" />
    <result column="view_count" jdbcType="BIGINT" property="viewCount" />
    <result column="like_count" jdbcType="BIGINT" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
    <result column="comment_count" jdbcType="BIGINT" property="commentCount" />
    <result column="topic" jdbcType="INTEGER" property="topic" />
  </resultMap>

  <update id="incView" parameterType="com.community.model.Question" >
    update QUESTION
    set
    VIEW_COUNT = VIEW_COUNT + #{viewCount, jdbcType=BIGINT}
    WHERE id = #{id}
  </update>

  <update id="incCommentCount" parameterType="com.community.model.Question" >
    update QUESTION
    set
    COMMENT_COUNT = COMMENT_COUNT + #{commentCount, jdbcType=BIGINT}
    WHERE id = #{id}
  </update>

  <select id="selectRelated" parameterType="com.community.model.Question" resultMap="BaseResultMap">
    select * from question
    where id != #{id} and tag regexp #{tag}
    limit 6
  </select>

  <select id="countBySearch" parameterType="com.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from question
    <where>
        <if test="search != null">
          and lower(title) regexp #{search}
        </if>
    </where>
  </select>

  <select id="selectBySearch" parameterType="com.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="search != null">
        and lower(title) regexp #{search}
      </if>
    </where>
    order by gmt_create desc limit #{offset}, #{size}
  </select>

  <select id="selectPopular"  resultMap="BaseResultMap">
    select * from question
    order by view_count desc limit 6
  </select>
</mapper>